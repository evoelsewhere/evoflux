interface:
  display_name: "Create and Edit Workbooks"
  short_description: "Build and verify editable Excel workbooks"
  default_prompt: "Use $xlsx to create or edit this workbook and verify every rendered sheet."
policy:
  allow_implicit_invocation: true
