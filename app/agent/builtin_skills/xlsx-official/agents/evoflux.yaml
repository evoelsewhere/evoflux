interface:
  display_name: "Spreadsheets"
  short_description: "Build, edit, and verify .xlsx and CSV files"
  default_prompt: "Use $xlsx-official to build this workbook with live formulas and verify it recalculates."
policy:
  allow_implicit_invocation: true
