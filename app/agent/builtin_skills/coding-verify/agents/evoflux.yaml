interface:
  display_name: "Verify Code"
  short_description: "Audit diffs, harden trust boundaries, design test proof"
  default_prompt: "Use $coding-verify to audit this for production-impacting defects without editing it."
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: builtin
      value: code_context
      description: "Unified indexed search, structural grep, exact-symbol graph navigation, and dynamic cross-repository resolution"
