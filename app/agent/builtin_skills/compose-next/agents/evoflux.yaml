interface:
  display_name: "Compose Next Delivery"
  short_description: "Spec-to-ship workflow for one feature, on request"
  default_prompt: "Use $compose-next to carry this change from requirements through review and hand-off."
policy:
  allow_implicit_invocation: false
