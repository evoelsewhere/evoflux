interface:
  display_name: "Implement Code Changes"
  short_description: "Ship focused changes with contract-level proof"
  default_prompt: "Use $coding-implementation to implement this scoped change and verify its observable contract."
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: builtin
      value: code_context
      description: "Unified indexed search, structural grep, exact-symbol graph navigation, and dynamic cross-repository resolution"
