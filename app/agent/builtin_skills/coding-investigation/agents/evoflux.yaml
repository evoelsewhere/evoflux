interface:
  display_name: "Investigate Code Paths"
  short_description: "Trace exact symbols, unknown roots, wiring, and ownership"
  default_prompt: "Use $coding-investigation to trace this behavior or exact symbol through native code context and bounded source evidence."
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: builtin
      value: code_context
      description: "Unified indexed search, structural grep, exact-symbol graph navigation, and dynamic cross-repository resolution"
