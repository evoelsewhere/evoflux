interface:
  display_name: "HTML to Video"
  short_description: "Render an HTML page to MP4 without silent defects"
  default_prompt: "Use $html-to-video-pipeline to build this render pipeline and verify the output."
policy:
  allow_implicit_invocation: true
