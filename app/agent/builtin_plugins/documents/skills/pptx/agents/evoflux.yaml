interface:
  display_name: "Create and Edit Presentations"
  short_description: "Build and verify editable PowerPoint decks"
  default_prompt: "Use $pptx to create or edit this presentation and verify every rendered slide."
policy:
  allow_implicit_invocation: true
