"""AIM (AI Innovation Modernization) service layer.

See ``documents/research/aim-framework.md`` for the design this implements.
The KB repo (git) is the system of record; ``app.models.aim`` tables are a
rebuildable local index (see :mod:`app.services.aim.reindex`).
"""
