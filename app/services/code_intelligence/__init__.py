"""Native exact-symbol resolution and structural graph traversal.

Parsers and index persistence remain in :mod:`app.services.code_graph`;
navigation does not interpret prose or fall through to MCP, grep, or LSP.
"""
