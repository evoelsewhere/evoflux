# EvoFlux Brand Assets

App-imported brand assets used by React components.

## Files

- `evoflux-app-icon.png` - geometric hexagon logo icon for app chrome / avatar tile
- `logo.svg` - canonical vector source (project root)

Marketing and documentation exports live in `documents/assets/brand/`. The browser-public copy in `web/public/brand-assets/` is only for direct URL needs such as the favicon.
