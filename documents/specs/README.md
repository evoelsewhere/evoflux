# Specifications

Accepted behavior-first specifications live here. Each published Run Spec uses
`<slug>--<owning-run-uuid>/`, containing an `index.yaml` and immutable files
under `revisions/`. Drafts stay inside their EASD Run until user approval.

An accepted revision is normative. Change it only by accepting another revision;
never edit the accepted file in place to match current code.
