# seed/commands/

Staging area for slash-command drafts. `commands.py` only walks project
`.evoflux/commands/` / `.opencode/commands/` and global
`{EVOFLUX_CONFIG_DIR}/commands/` / `~/.config/opencode/commands/` today —
nothing under `seed/commands/` is auto-discovered until copied into one of
those roots.
