# Construct mapping example

Copy this file to a project-specific name and record reviewed mappings:

| Legacy construct | Target pattern | Constraints | Rule/ADR |
|---|---|---|---|
| `EXAMPLE_CALL` | `TargetService.call()` | Preserve error semantics | `ADR-0000` |

Mappings guide `aim-target-architect`; they do not perform a transformation.