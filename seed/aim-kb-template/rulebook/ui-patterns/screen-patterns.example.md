# Screen-pattern mapping example

| Legacy interaction | Target template | Acceptance behavior |
|---|---|---|
| Search + result grid | `SearchListPage` | Same filters, ordering, and actions |

Copy to a project-specific file and declare `ui_patterns` in `rulebook.yaml`.