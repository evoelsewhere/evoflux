# UI / UX conventions

Only relevant if this migration includes screens. Decided **once**, by `aim-target-architect`, before any screen is converted — see the `aim-ui-conventions` skill. Every per-unit mapping cites this file; it does not get re-decided per screen.

## Design system

_(the target UI kit — component library, layout templates, conventions for validation/error/empty states, i18n)_

## Screen pattern mapping

_(reference `ui-patterns/` in the project's rulebook for the legacy-pattern → target-template mapping; list the patterns actually observed in this estate here — search-list, detail-edit, master-detail, wizard, report, ...)_

## UX deviations from legacy (ADRs)

_(list of `decisions/ADR-###.md` that record intentional behavior changes from the legacy screens — e.g. menu → navigation bar, multi-window → tabs)_
